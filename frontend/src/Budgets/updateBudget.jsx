import { useAuth } from "../contexts/AuthContext";
import { useState, useEffect } from "react";
import axios from "axios";

const updateBudget = () => {
  //* POST : Fonction pour modifier un budget
  const updateBudgets = async () => {};
};

export default updateBudget;
