import { useAuth } from "../contexts/AuthContext";
import { useState, useEffect } from "react";
import axios from "axios";

const deleteBudget = () => {
  //* DELETE : Fonction pour supprimer un budget
  const deleteBudgets = async () => {};
};

export default deleteBudget;
